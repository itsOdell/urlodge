import { NextPage } from "next/types";
import styles from "src/styles/User.module.css";

const User: NextPage = (): React.ReactElement => {
    return (
        <p>hello</p>
    )
}

export default User